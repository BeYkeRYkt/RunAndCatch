
public enum GameRoomState
{
    // Room is null
    DISCONNECTED = -1,

    // Room is waiting
    WAITING = 0,

    // Room is starting
    STARTING = 1,

    // Room is running
    RUNNING = 2,

    // Room is end
    ENDING = 3
}
